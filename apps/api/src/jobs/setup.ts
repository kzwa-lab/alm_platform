// Cron jobs placeholder - will be populated with data ingestion jobs
import { logger } from '../utils/logger.js';

export function setupJobs() {
  logger.info('Cron jobs setup complete (no active jobs in dev mode)');
}
